<!-- File: res/values/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Calculator" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Для обратной совместимости (оставляем ваш вариант) -->
        <item name="android:statusBarColor">@color/status_bar_color</item>

        <!-- Делаем иконки на строке состояния СВЕТЛЫМИ (белыми) -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>



    </style>


    <style name="CustomSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textColorHighlight">#80FF0000</item>

        <item name="colorControlActivated">#FF0000</item>

        <!-- Цвет самого текста -->
        <item name="android:textColor">@color/black</item>

        <!-- Цвет подсказки (hint) -->
        <item name="android:textColorHint">#878787</item>
    </style>


    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />
</resources>
